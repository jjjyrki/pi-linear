import { createPlaceholderTool } from './placeholder.js';

export const linearListWorkflowStatesTool = createPlaceholderTool(
  'linear_list_workflow_states',
  'List Workflow States',
  'Placeholder Linear tool for listing workflow states.',
);
