import { createPlaceholderTool } from './placeholder.js';

export const linearViewerTool = createPlaceholderTool(
  'linear_viewer',
  'Viewer',
  'Placeholder Linear tool for reading the authenticated user.',
);
