import { createPlaceholderTool } from './placeholder.js';

export const linearListTeamsTool = createPlaceholderTool(
  'linear_list_teams',
  'List Teams',
  'Placeholder Linear tool for listing teams.',
);
